<!DOCTYPE html>
html(lang="ru")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    link(rel="apple-touch-icon" sizes="180x180" href="../../images/apple-touch-icon.png")
    link(rel="icon" type="image/png" sizes="32x32" href="../../images/favicon-32x32.png")
    link(rel="icon" type="image/png" sizes="16x16" href="../../images/favicon-16x16.png")
    link(rel="mask-icon" href="../../images/safari-pinned-tab.svg" color="#5bbad5")
    meta(name="msapplication-TileColor" content="#da532c")
    meta(name="theme-color" content="#ffffff")
    link(rel="shortcut icon" href="../../images/favicon.ico")
    link(rel='stylesheet', href='./Profile.scss') 
    title Pigeon - Profile
  body
    .page
      aside.profile__back
        a.profile__backButton(href="../MainPage/MainPage.pug")
      section.profile__main
        image.profile__img
        h2.profile__title Регистрация
        form.login__form
          fieldset.login__fields
            p.login__input-name Почта
            input.login__input(
              type="email"
              name="email"
              pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$"
              required
              placeholder="pochta@pochta.com"
              disabled
            )
            span.login__error

            p.login__input-name Логин
            input.login__input(
              type="text"
              name="login"
              required
              minLength="2"
              placeholder="Mylogin"
              disabled
            )
            span.login__error

            p.login__input-name Имя
            input.login__input(
              type="text"
              name="name"
              required
              placeholder="Иван"
              disabled

            )
            span.login__error

            p.login__input-name Фамилия
            input.login__input(
              type="text"
              name="lastname"
              required
              placeholder="Иванов"
              disabled
            )
            span.login__error

            p.login__input-name Телефон
            input.login__input(
              type="tel"
              name="phone"
              required
              placeholder="+79111111111"
              disabled 
            )
            span.login__error

            p.login__input-name Пароль
            input.login__input(
              type="password"
              name="password"
              required
              minLength="8"
              disabled 
            )
            span.login__error

          span.login__submit-error
          button.login__submit-button(type="submit") Изменить данные
          button.login__submit-button(type="submit") Изменить пароль 
          a.login__subtitle(href="#") Выйти